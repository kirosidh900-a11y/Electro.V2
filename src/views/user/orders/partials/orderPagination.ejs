<% for(let i = 1; i <= totalPages; i++) { %>
  <button
    class="pagination-link px-3 py-1 border rounded <%= currentPage === i ? 'bg-blue-600 text-white' : '' %>"
    data-page="<%= i %>">
    <%= i %>
  </button>
<% } %>