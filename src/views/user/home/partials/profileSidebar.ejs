<!-- SIDEBAR -->
<aside class="w-full lg:w-1/4 bg-white rounded-xl shadow-sm overflow-hidden h-fit lg:sticky lg:top-6">

    <!-- USER -->
    <div class="p-6 flex items-center gap-4 border-b border-gray-100">

        <div class="h-12 w-12 rounded-full overflow-hidden">
            <img id="sidebarImg"
                src="<%= user?.photo || 'https://res.cloudinary.com/dxtiklfxy/image/upload/v1773824246/profile/qhvfjaqer1o81nwkxvcz.png' %>"
                class="w-full h-full object-cover">
        </div>

        <div>
            <p class="text-xs text-gray-400">Hello,</p>
            <h2 id="sidebarName" class="font-semibold text-gray-800">
                <%= user?.name %>
            </h2>
        </div>

    </div>

    <!-- NAV -->
    <nav class="p-4 space-y-1">

        <!-- ORDERS -->
        <a href="/orders"
            class="flex items-center justify-between p-3 rounded-lg font-medium transition
            <%= currentRoute === '/orders'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            <span class="flex items-center gap-3">
                <i class="fa-solid fa-box"></i> My Orders
            </span>

            <i class="fa-solid fa-chevron-right text-xs"></i>
        </a>

        <!-- ACCOUNT SETTINGS -->
        <p class="text-[10px] font-bold text-gray-400 uppercase px-3 pt-5 pb-2">
            Account Settings
        </p>

        <!-- PROFILE -->
        <a href="/myProfile"
            class="block p-3 rounded-lg font-medium transition
            <%= currentRoute === '/myProfile'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            Profile Information
        </a>

        <!-- ADDRESS -->
        <a href="/address"
            class="block p-3 rounded-lg font-medium transition
            <%= currentRoute === '/address'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            Manage Addresses
        </a>

        <!-- REFERRAL -->
        <a href="/referral"
            class="block p-3 rounded-lg font-medium transition
            <%= currentRoute === '/referral'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            My Referral Info
        </a>

        <!-- MY STUFF -->
        <p class="text-[10px] font-bold text-gray-400 uppercase px-3 pt-5 pb-2">
            My Stuff
        </p>

        <a href="/coupons"
            class="block p-3 rounded-lg transition
            <%= currentRoute === '/coupons'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            My Coupons
        </a>

        <a href="/reviews"
            class="block p-3 rounded-lg transition
            <%= currentRoute === '/reviews'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            My Reviews & Ratings
        </a>

        <a href="/wishlist"
            class="block p-3 rounded-lg transition
            <%= currentRoute === '/wishlist'
            ? 'text-blue-600 bg-blue-50'
            : 'text-gray-600 hover:bg-gray-50' %>">

            My Wishlist
        </a>

        <!-- LOGOUT -->
        <div class="mt-4 pt-4 border-t border-gray-100">
            <a id="logout"
                class="flex items-center gap-3 p-3 text-red-500 hover:bg-red-50 rounded-lg transition">
                <i class="fa-solid fa-power-off"></i> Logout
            </a>
        </div>

    </nav>

</aside>