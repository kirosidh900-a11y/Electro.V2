<% if (totalPages > 1) { %>
<%
  const WINDOW = 2;
  const pages  = [];

  pages.push(1);
  if (currentPage - WINDOW > 2)          pages.push(null);
  for (let i = Math.max(2, currentPage - WINDOW); i <= Math.min(totalPages - 1, currentPage + WINDOW); i++) {
    pages.push(i);
  }
  if (currentPage + WINDOW < totalPages - 1) pages.push(null);
  if (totalPages > 1) pages.push(totalPages);
%>

<div class="flex items-center justify-center gap-1.5 mt-8 flex-wrap">

  <%# Prev — no href, JS handles navigation to preserve search/filter state %>
  <a data-page="<%= currentPage > 1 ? currentPage - 1 : 1 %>"
     class="pagination-link flex items-center gap-1.5 px-3 py-2 rounded-lg text-xs font-medium transition cursor-pointer
            <%= currentPage === 1 ? 'opacity-30 pointer-events-none bg-white/5 text-gray-500' : 'bg-white/5 hover:bg-white/10 text-gray-300' %>">
    <i class="fas fa-chevron-left text-[10px]"></i> Prev
  </a>

  <% pages.forEach(p => { %>
    <% if (p === null) { %>
      <span class="px-2 py-2 text-gray-600 text-xs select-none">•••</span>
    <% } else { %>
      <a data-page="<%= p %>"
         class="pagination-link pagination-number w-9 h-9 flex items-center justify-center rounded-lg text-sm font-semibold transition cursor-pointer
                <%= currentPage === p
                  ? 'bg-cyan-600 text-white shadow-lg shadow-cyan-900/30'
                  : 'bg-white/5 hover:bg-white/10 text-gray-300' %>">
        <%= p %>
      </a>
    <% } %>
  <% }) %>

  <%# Next — no href %>
  <a data-page="<%= currentPage < totalPages ? currentPage + 1 : totalPages %>"
     class="pagination-link flex items-center gap-1.5 px-3 py-2 rounded-lg text-xs font-medium transition cursor-pointer
            <%= currentPage === totalPages ? 'opacity-30 pointer-events-none bg-white/5 text-gray-500' : 'bg-white/5 hover:bg-white/10 text-gray-300' %>">
    Next <i class="fas fa-chevron-right text-[10px]"></i>
  </a>

</div>
<% } %>
