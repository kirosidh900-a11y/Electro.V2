<!DOCTYPE html>
<html lang="en" class="dark">

<head>
        <%- include('../../partials/head') %>
                <link rel="stylesheet" href="https://unpkg.com/cropperjs@1.6.2/dist/cropper.min.css">
                <script src="https://unpkg.com/cropperjs@1.6.2/dist/cropper.min.js"></script>

</head>

<body class="min-h-screen flex">

        <%- include('../../partials/admin/sidebar') %>

                <div id="mainContent" class="flex-1 flex flex-col min-w-0 transition-all duration-300">

                        <%- include('../../partials/admin/header') %>

                                <main class="flex-1 p-6">

                                        <!-- ================= HEADER ================= -->

                                        <div class="flex items-center justify-between mb-6">

                                                <button onclick="history.back()"
                                                        class="text-sm text-blue-400 hover:text-white transition">
                                                        ← Back To Products
                                                </button>

                                                <h1 class="text-xl font-semibold text-white">
                                                        Product Details
                                                </h1>

                                                <!-- ACTION DROPDOWN -->
                                                <div class="relative">

                                                        <!-- <button onclick="toggleActionMenu()"
                                                                class="bg-gray-700 hover:bg-gray-600 text-white text-sm px-4 py-2 rounded-lg flex items-center gap-2">

                                                                Action
                                                                <i class="fas fa-chevron-down text-xs"></i>

                                                        </button> -->

                                                        <!-- DROPDOWN -->
                                                        <div id="actionDropdown"
                                                                class="absolute right-0 mt-2 w-44 bg-[#0f172a] border border-white/10 rounded-lg shadow-lg hidden">

                                                                <!-- EDIT PRODUCT -->
                                                                <!-- <a href="/admin/products/edit/<%= product._id %>"
                                                                        class="block px-4 py-2 text-sm text-gray-300 hover:bg-white/10">
                                                                        Edit Product
                                                                </a> -->

                                                                <!-- LIST / UNLIST -->
                                                                <!-- <% if(product.status==="listed" ){ %>

                                                                        <button onclick="toggleProductStatus('<%= product._id %>','unlist')"
                                                                                class="w-full text-left px-4 py-2 text-sm text-yellow-400 hover:bg-white/10">
                                                                                Unlist Product
                                                                        </button>

                                                                        <% } else { %>

                                                                                <button onclick="toggleProductStatus('<%= product._id %>','list')"
                                                                                        class="w-full text-left px-4 py-2 text-sm text-green-400 hover:bg-white/10">
                                                                                        List Product
                                                                                </button>

                                                                                <% } %> -->

                                                                <!-- DELETE PRODUCT -->
                                                                <button onclick="deleteProduct('<%= product._id %>')"
                                                                        class="w-full text-left px-4 py-2 text-sm text-red-400 hover:bg-white/10">
                                                                        Delete Product
                                                                </button>

                                                        </div>

                                                </div>

                                        </div>


                                        <!-- ================= PRODUCT INFO ================= -->

                                        <div class="grid grid-cols-2 gap-6 mb-6">

                                                <!-- LEFT CARD -->

                                                <div class="bg-[#0b1220] border border-white/10 rounded-xl p-6">

                                                        <h3 class="text-white font-semibold mb-4">
                                                                Product Information
                                                        </h3>

                                                        <div class="space-y-2 text-sm text-gray-300">

                                                                <p>
                                                                        <span class="text-gray-400">Name:</span>
                                                                        <%= product.name %>
                                                                </p>

                                                                <p>
                                                                        <span class="text-gray-400">Brand:</span>
                                                                        <%= product.brand.title %>
                                                                </p>

                                                                <p>
                                                                        <span class="text-gray-400">Category:</span>
                                                                        <%= product.category.title %>
                                                                </p>

                                                                <p>
                                                                        <span class="text-gray-400">Status:</span>

                                                                        <span class="px-2 py-1 rounded-full text-xs
                                                              <%= product.status === " listed"
                                                                                ? "bg-emerald-500/20 text-emerald-400"
                                                                                : "bg-red-500/20 text-red-400" %>">

                                                                                <%= product.status==="listed" ? "Listed"
                                                                                        : "Unlisted" %>

                                                                        </span>
                                                                </p>

                                                                <p>
                                                                        <span class="text-gray-400">Created:</span>
                                                                        <%= product.createdAt.toDateString() %>
                                                                </p>

                                                                <p>
                                                                        <span class="text-gray-400">Updated:</span>
                                                                        <%= product.updatedAt.toDateString() %>
                                                                </p>

                                                        </div>

                                                </div>


                                                <!-- RIGHT CARD -->

                                                <div class="bg-[#0b1220] border border-white/10 rounded-xl p-6">

                                                        <h3 class="text-white font-semibold mb-4">
                                                                Product Metrics
                                                        </h3>

                                                        <div class="grid grid-cols-2 gap-4">

                                                                <div class="bg-[#0f172a] p-4 rounded-lg text-center">
                                                                        <p class="text-gray-400 text-sm">Total Variants
                                                                        </p>
                                                                        <p class="text-xl text-white font-semibold">
                                                                                <%= totalVariants %>
                                                                        </p>
                                                                </div>

                                                                <div class="bg-[#0f172a] p-4 rounded-lg text-center">
                                                                        <p class="text-gray-400 text-sm">Minimum Price
                                                                        </p>
                                                                        <p class="text-xl text-white font-semibold">₹<%=
                                                                                        minPrice %>
                                                                        </p>
                                                                </div>

                                                                <div class="bg-[#0f172a] p-4 rounded-lg text-center">
                                                                        <p class="text-gray-400 text-sm">Total Stock</p>
                                                                        <p class="text-xl text-white font-semibold">
                                                                                <%= totalStock %>
                                                                        </p>
                                                                </div>

                                                                <div class="bg-[#0f172a] p-4 rounded-lg text-center">
                                                                        <p class="text-gray-400 text-sm">In Stock</p>
                                                                        <p class="text-xl text-white font-semibold">
                                                                                <%= totalStock> 0 ? "Yes" : "No" %>
                                                                        </p>
                                                                </div>

                                                        </div>

                                                </div>

                                        </div>


                                        <!-- ================= VARIANTS ================= -->

                                        <div class="bg-[#0b1220] border border-white/10 rounded-xl p-6">

                                                <div class="flex items-center justify-between mb-4">

                                                        <h3 class="text-white font-semibold">
                                                                Variants (<%= variants.length %>)
                                                        </h3>

                                                        <button onclick="openVariantModal()"
                                                                class="bg-blue-600 hover:bg-blue-700 text-white text-sm px-4 py-2 rounded-lg">
                                                                + Add Variant
                                                        </button>

                                                </div>

                                                <% let attributeKeys=[]; variants.forEach(function(v){ const
                                                        attrs=v.attributes instanceof Map ?
                                                        Object.fromEntries(v.attributes) : (v.attributes || {});
                                                        Object.keys(attrs).forEach(function(key){
                                                        if(!attributeKeys.includes(key)){ attributeKeys.push(key) } })
                                                        }) %>


                                                        <% if(variants.length===0){ %>

                                                                <div class="text-center text-gray-400 py-10">

                                                                        <p>No variants added yet</p>

                                                                        <button onclick="openVariantModal()"
                                                                                class="mt-3 bg-blue-600 px-4 py-2 rounded-lg text-sm text-white">
                                                                                Add First Variant
                                                                        </button>

                                                                </div>

                                                                <% } else { %>

                                                                        <div class="overflow-x-auto">
                                                                                <table
                                                                                        class="min-w-full text-sm text-gray-300">

                                                                                        <thead
                                                                                                class="border-b border-white/10 text-gray-400">
                                                                                                <tr>

                                                                                                        <th
                                                                                                                class="py-3 text-left">
                                                                                                                SKU
                                                                                                        </th>

                                                                                                        <%
                                                                                                                attributeKeys.forEach(key=>
                                                                                                                {
                                                                                                                %>
                                                                                                                <th
                                                                                                                        class="py-3 text-left">
                                                                                                                        <%= key.toUpperCase()
                                                                                                                                %>
                                                                                                                </th>
                                                                                                                <% }) %>

                                                                                                                        <th
                                                                                                                                class="py-3 text-left">
                                                                                                                                Price
                                                                                                                        </th>
                                                                                                                        <th
                                                                                                                                class="py-3 text-left">
                                                                                                                                Stock
                                                                                                                        </th>
                                                                                                                        <th
                                                                                                                                class="py-3 text-left">
                                                                                                                                Images
                                                                                                                        </th>
                                                                                                                        <th
                                                                                                                                class="py-3 text-right">
                                                                                                                                Actions
                                                                                                                        </th>

                                                                                                </tr>
                                                                                        </thead>

                                                                                        <tbody>

                                                                                                <% variants.forEach(v=>
                                                                                                        { %>

                                                                                                        <tr
                                                                                                                class="border-b border-white/5">

                                                                                                                <td
                                                                                                                        class="py-3">
                                                                                                                        <%= v.sku
                                                                                                                                %>
                                                                                                                </td>

                                                                                                                <%
                                                                                                                        attributeKeys.forEach(key=>
                                                                                                                        {
                                                                                                                        %>

                                                                                                                        <td
                                                                                                                                class="py-3">
                                                                                                                                <%= v.attributes?.get
                                                                                                                                        ?
                                                                                                                                        v.attributes.get(key)
                                                                                                                                        :
                                                                                                                                        v.attributes?.[key]
                                                                                                                                        || "-"
                                                                                                                                        %>
                                                                                                                        </td>

                                                                                                                        <% })
                                                                                                                                %>

                                                                                                                                <td>₹<%= v.price
                                                                                                                                                %>
                                                                                                                                </td>
                                                                                                                                <td>
                                                                                                                                        <%= v.stock
                                                                                                                                                %>
                                                                                                                                </td>


                                                                                                                                <!-- IMAGES COLUMN -->

                                                                                                                                <td
                                                                                                                                        class="py-3">

                                                                                                                                        <div
                                                                                                                                                class="flex flex-col items-center gap-2">

                                                                                                                                                <% if(v.product_images
                                                                                                                                                        &&
                                                                                                                                                        v.product_images.length){
                                                                                                                                                        %>

                                                                                                                                                        <%
                                                                                                                                                                v.product_images.forEach(img=>
                                                                                                                                                                {
                                                                                                                                                                %>

                                                                                                                                                                <div
                                                                                                                                                                        class="relative group">

                                                                                                                                                                        <img src="<%= img.url %>"
                                                                                                                                                                                class="w-12 h-12 object-cover rounded border border-white/10" />

                                                                                                                                                                        <button onclick='deleteVariantImage("<%= v._id %>", <%- JSON.stringify(img) %>)'
                                                                                                                                                                                class="absolute -top-2 -right-2 bg-red-500 text-white rounded-full w-4 h-4 text-[10px] hidden group-hover:flex items-center justify-center">
                                                                                                                                                                                ×
                                                                                                                                                                        </button>

                                                                                                                                                                </div>

                                                                                                                                                                <% })
                                                                                                                                                                        %>

                                                                                                                                                                        <% }
                                                                                                                                                                                %>


                                                                                                                                                                                <!-- ADD IMAGE BOX -->

                                                                                                                                                                                <div onclick="openImageUpload('<%= v._id %>')"
                                                                                                                                                                                        class="w-12 h-12 border-2 border-dashed border-gray-500 rounded flex flex-col items-center justify-center cursor-pointer hover:border-blue-500">

                                                                                                                                                                                        <i
                                                                                                                                                                                                class="fas fa-plus text-xs"></i>
                                                                                                                                                                                        <span
                                                                                                                                                                                                class="text-[9px]">Add</span>

                                                                                                                                                                                </div>

                                                                                                                                        </div>

                                                                                                                                </td>


                                                                                                                                <td
                                                                                                                                        class="text-right">

                                                                                                                                        <div
                                                                                                                                                class="flex justify-end gap-3">

                                                                                                                                                <!-- EDIT -->

                                                                                                                                                <button onclick='editVariant(
                        "<%= v._id %>",
                        "<%= v.sku %>",
                        "<%= v.price %>",
                        "<%= v.stock %>",
                        <%- JSON.stringify(v.attributes instanceof Map ? Object.fromEntries(v.attributes) : v.attributes) %>
                )' class="text-blue-400 text-xs hover:text-blue-300">

                                                                                                                                                        Edit

                                                                                                                                                </button>


                                                                                                                                                <!-- DELETE -->

                                                                                                                                                <button onclick="deleteVariant('<%= v._id %>')"
                                                                                                                                                        class="text-red-400 text-xs hover:text-red-300">

                                                                                                                                                        Delete

                                                                                                                                                </button>

                                                                                                                                        </div>

                                                                                                                                </td>
                                                                                                                                <input type="file"
                                                                                                                                        id="variantImageInput"
                                                                                                                                        class="hidden"
                                                                                                                                        accept="image/*">
                                                                                                        </tr>

                                                                                                        <% }) %>

                                                                                        </tbody>

                                                                                </table>
                                                                        </div>

                                                                        <% } %>

                                        </div>

                                </main>

                </div>

                <div id="variantModal" class="fixed inset-0 bg-black/60 hidden z-50 flex items-center justify-center">

                        <div class="bg-[#0b1220] w-[550px] rounded-xl border border-white/10 p-6">

                                <div class="flex justify-between items-center mb-4">

                                        <h2 id="variantModalTitle" class="text-lg font-semibold text-white">
                                                Add Variant
                                        </h2>

                                        <button onclick="closeVariantModal()" class="text-gray-400 hover:text-white">

                                                <i class="fas fa-times"></i>

                                        </button>

                                </div>


                                <form id="variantForm" class="flex flex-col gap-4">

                                        <input type="hidden" id="variant-id">


                                        <!-- SKU -->

                                        <div>

                                                <label class="text-sm text-gray-400">SKU</label>

                                                <input type="text" id="variant-sku"
                                                        class="w-full mt-1 bg-[#0f172a] border border-white/10 px-3 py-2 rounded-lg text-sm">

                                        </div>


                                        <!-- PRICE -->

                                        <div>

                                                <label class="text-sm text-gray-400">Price</label>

                                                <input type="number" id="variant-price"
                                                        class="w-full mt-1 bg-[#0f172a] border border-white/10 px-3 py-2 rounded-lg text-sm">

                                        </div>


                                        <!-- STOCK -->

                                        <div>

                                                <label class="text-sm text-gray-400">Stock</label>

                                                <input type="number" id="variant-stock"
                                                        class="w-full mt-1 bg-[#0f172a] border border-white/10 px-3 py-2 rounded-lg text-sm">

                                        </div>


                                        <!-- VARIANT ATTRIBUTES (Dynamic) -->

                                        <div>

                                                <label class="text-sm text-gray-400">
                                                        Variant Attributes
                                                </label>

                                                <div id="variantAttributes" class="flex flex-col gap-2"></div>

                                        </div>


                                        <!-- BUTTONS -->

                                        <div class="flex justify-center gap-3 mt-4">

                                                <button type="button" onclick="closeVariantModal()"
                                                        class="px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white rounded-lg text-sm">

                                                        Cancel

                                                </button>


                                                <button type="submit"
                                                        class="px-4 py-2 bg-cyan-600 hover:bg-cyan-700 text-white rounded-lg text-sm">

                                                        Save Variant

                                                </button>

                                        </div>

                                </form>

                        </div>

                </div>
                <!-- croper modal -->
                <div id="variantCropModal"
                        class="fixed inset-0 bg-black/70 hidden z-50 flex items-center justify-center">

                        <div class="bg-[#0b1220] p-6 rounded-xl w-[600px]">

                                <h2 class="text-white mb-4">Crop Image</h2>

                                <div class="max-h-[400px]">
                                        <img id="variantCropImage" class="max-w-full">
                                </div>

                                <div class="flex justify-end gap-3 mt-4">
                                        <button onclick="closeVariantCrop()" class="px-4 py-2 bg-gray-600 rounded">
                                                Cancel
                                        </button>

                                        <button onclick="cropVariantImage()" class="px-4 py-2 bg-cyan-600 rounded">
                                                Crop
                                        </button>
                                </div>

                        </div>

                </div>

                <script type="module">

                        import { showToast } from "../../partials/errorMsg.utils.js";
                        import { confirmAction } from "../../partials/confirm.util.js";

                        let editingVariantAttributes = {};
                        let currentVariantId = null;

                        // 🔥 Cropper variables
                        let variantCropper;
                        let variantCroppedBlob;

                        const variantImageInput = document.getElementById("variantImageInput");
                        const variantCropModal = document.getElementById("variantCropModal");
                        const variantCropImage = document.getElementById("variantCropImage");

                        /* ================= ACTION MENU ================= */

                        window.toggleActionMenu = function () {
                                const menu = document.getElementById("actionDropdown");
                                menu.classList.toggle("hidden");
                        };

                        document.addEventListener("click", function (e) {
                                const menu = document.getElementById("actionDropdown");
                                if (!e.target.closest(".relative")) {
                                        menu?.classList.add("hidden");
                                }
                        });

                        /* ================= VARIANT MODAL ================= */

                        window.openVariantModal = function () {
                                document.getElementById("variant-id").value = "";
                                document.getElementById("variant-sku").value = "";
                                document.getElementById("variant-price").value = "";
                                document.getElementById("variant-stock").value = "";

                                editingVariantAttributes = {};

                                document.getElementById("variantModalTitle").textContent = "Add Variant";

                                loadVariantAttributes();

                                document.getElementById("variantModal").classList.remove("hidden");
                        };

                        window.closeVariantModal = function () {
                                document.getElementById("variantModal").classList.add("hidden");
                        };

                        /* ================= LOAD ATTRIBUTES ================= */

                        async function loadVariantAttributes(existingAttributes = {}) {
                                const categoryId = "<%= product.category._id %>";
                                const container = document.getElementById("variantAttributes");

                                container.innerHTML = "";

                                try {
                                        const res = await fetch(`/admin/category/${categoryId}/attributes`);
                                        const result = await res.json();

                                        result.data.variantAttributes.forEach(attr => {
                                                const input = document.createElement("input");

                                                input.placeholder = attr.label;
                                                input.name = attr.key;
                                                input.value = existingAttributes[attr.key] || "";

                                                input.className =
                                                        "bg-[#0f172a] border border-white/10 px-3 py-2 rounded-lg text-sm";

                                                container.appendChild(input);
                                        });

                                } catch {
                                        showToast("Failed to load attributes", "error");
                                }
                        }

                        /* ================= EDIT VARIANT ================= */

                        window.editVariant = function (id, sku, price, stock, attributes = {}) {
                                document.getElementById("variant-id").value = id;
                                document.getElementById("variant-sku").value = sku;
                                document.getElementById("variant-price").value = price;
                                document.getElementById("variant-stock").value = stock;

                                editingVariantAttributes = attributes || {};

                                document.getElementById("variantModalTitle").textContent = "Edit Variant";

                                loadVariantAttributes(editingVariantAttributes);

                                document.getElementById("variantModal").classList.remove("hidden");
                        };

                        /* ================= SUBMIT VARIANT ================= */

                        const variantForm = document.getElementById("variantForm");

                        variantForm.addEventListener("submit", async (e) => {
                                e.preventDefault();

                                try {
                                        const variantId = document.getElementById("variant-id").value;
                                        const sku = document.getElementById("variant-sku").value.trim();
                                        const price = parseFloat(document.getElementById("variant-price").value);
                                        const stock = parseInt(document.getElementById("variant-stock").value);

                                        if (!sku) return showToast("SKU is required", "warning");
                                        if (sku.length < 3) return showToast("SKU must be at least 3 characters", "warning");
                                        if (!price || price <= 0) return showToast("Price must be greater than 0", "warning");
                                        if (stock < 0) return showToast("Stock cannot be negative", "warning");

                                        const attributes = {};

                                        document.querySelectorAll("#variantAttributes input").forEach(input => {
                                                attributes[input.name] = input.value.trim();
                                        });

                                        for (const key in attributes) {
                                                if (!attributes[key]) return showToast(`${key} cannot be empty`, "warning");
                                        }

                                        let url = `/admin/products/<%= product._id %>/variants`;
                                        let method = "POST";

                                        if (variantId) {
                                                url = `/admin/products/<%= product._id %>/variants/${variantId}`;
                                                method = "PATCH";
                                        }

                                        const res = await fetch(url, {
                                                method,
                                                headers: { "Content-Type": "application/json" },
                                                body: JSON.stringify({ sku, price, stock, attributes })
                                        });

                                        const data = await res.json();

                                        if (!data.success) return showToast(data.message, "error");

                                        showToast(data.message, "success");

                                        setTimeout(() => location.reload(), 1200);

                                } catch {
                                        showToast("Something went wrong", "error");
                                }
                        });

                        /* ================= DELETE VARIANT ================= */

                        window.deleteVariant = async function (id) {
                                const confirmed = await confirmAction("Delete Variant?");
                                if (!confirmed) return;

                                try {
                                        const res = await fetch(
                                                `/admin/products/<%= product._id %>/variants/${id}`,
                                                { method: "DELETE" }
                                        );

                                        const data = await res.json();

                                        if (!data.success)
                                                return showToast(data.message, "error");

                                        showToast("Variant deleted successfully", "success");

                                        setTimeout(() => location.reload(), 1200);

                                } catch {
                                        showToast("Delete failed", "error");
                                }
                        };

                        /* ================= IMAGE UPLOAD (WITH CROPPER) ================= */

                        window.openImageUpload = function (variantId) {
                                currentVariantId = variantId;
                                variantImageInput.click();
                        };

                        variantImageInput.addEventListener("change", function () {
                                const file = this.files[0];
                                if (!file) return;

                                if (!file.type.startsWith("image/"))
                                        return showToast("Only image files allowed", "warning");

                                if (file.size > 5 * 1024 * 1024)
                                        return showToast("Image must be less than 5MB", "warning");

                                const reader = new FileReader();

                                reader.onload = function (e) {
                                        variantCropImage.src = e.target.result;
                                        variantCropModal.classList.remove("hidden");

                                        setTimeout(() => {
                                                if (variantCropper) variantCropper.destroy();

                                                variantCropper = new Cropper(variantCropImage, {
                                                        viewMode: 1,
                                                        autoCropArea: 1,
                                                        responsive: true,
                                                        background: false,
                                                });
                                        }, 100);
                                };

                                reader.readAsDataURL(file);
                        });

                        /* ================= CROP & UPLOAD ================= */

                        window.cropVariantImage = async function () {
                                if (!variantCropper) return;

                                const canvas = variantCropper.getCroppedCanvas({
                                        width: 400,
                                        height: 400
                                });

                                canvas.toBlob(async (blob) => {

                                        const formData = new FormData();
                                        formData.append("image", blob, "variant.png");

                                        try {
                                                const res = await fetch(
                                                        `/admin/products/<%= product._id %>/variants/${currentVariantId}/image`,
                                                        {
                                                                method: "POST",
                                                                body: formData
                                                        }
                                                );

                                                const data = await res.json();

                                                if (!data.success)
                                                        return showToast(data.message, "error");

                                                showToast("Image uploaded successfully", "success");

                                                closeVariantCrop();

                                                setTimeout(() => location.reload(), 1000);

                                        } catch {
                                                showToast("Upload failed", "error");
                                        }

                                }, "image/png");
                        };

                        /* ================= CLOSE CROP ================= */

                        window.closeVariantCrop = function () {
                                variantCropModal.classList.add("hidden");

                                if (variantCropper) {
                                        variantCropper.destroy();
                                        variantCropper = null;
                                }

                                variantImageInput.value = "";
                        };

                        /* ================= DELETE IMAGE ================= */

                        window.deleteVariantImage = async function (variantId, img) {
                                const confirmed = await confirmAction("Delete image?");
                                if (!confirmed) return;

                                try {
                                        const res = await fetch(
                                                `/admin/products/<%= product._id %>/variants/${variantId}/image`,
                                                {
                                                        method: "DELETE",
                                                        headers: { "Content-Type": "application/json" },
                                                        body: JSON.stringify({ imageId: img.imageId })
                                                }
                                        );

                                        const data = await res.json();

                                        if (!data.success)
                                                return showToast(data.message, "error");

                                        showToast("Image removed", "success");

                                        setTimeout(() => location.reload(), 1200);

                                } catch {
                                        showToast("Delete failed", "error");
                                }
                        };

                </script>
</body>

</html>